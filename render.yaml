services:
  - type: web
    name: meal-analyzer-04-lab
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 4 --timeout 120 server-meal-analyzer-04-lab:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
